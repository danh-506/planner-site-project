// main.js
// Entry point: importing the component files registers the custom elements.
// Add any global wiring here later (e.g. an "add event" form submit handler).

import './components/main-calendar.js';
import './components/event-box.js';