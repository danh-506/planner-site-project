// constants.js
// Single source of truth for values shared across modules — avoids
// calendar.js, events.js, and the components drifting out of sync
// on what "all day" or the default color actually is.

export const ALL_DAY_LABEL = 'All day';
export const DEFAULT_COLOR = '#808080';